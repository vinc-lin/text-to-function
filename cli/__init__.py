"""The interactive session: type Chinese, watch the Central Model workflow run.

`session.py` is the logic (one utterance in, one structured Turn out), `render.py` turns a
Turn into text, and `__main__.py` does the reading and printing.
"""
